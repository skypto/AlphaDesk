import { CredentialSettings } from "@/components/credential-settings";
import { WorkspaceShell } from "@/components/workspace-shell";
export default function Settings(){return <WorkspaceShell mode="desk" title="Credential Settings" description="Test, encrypt, replace, or delete tenant-scoped external provider access."><CredentialSettings/></WorkspaceShell>}
