# Database migrations

Alembic migrations define AlphaDesk's authoritative internal event store and
transactional outbox. Projections remain rebuildable from events and future
broker reconciliation snapshots.

